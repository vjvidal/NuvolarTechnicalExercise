# NuvolarTechnicalExercise
First of all it's necessary to deploy the two custom objects nv_obj_flight__c and nv_obj_Airport__c (inside the object folder).
Once these two objects are deployed, you have to deploy the apexClasses (inside the classes object).
Finally you can deploy the LWC in your org, add to a Salesforce page and test it.
